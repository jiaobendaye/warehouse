# Local data directory (SQLite + logs)
data/

# Wails build output
build/
warehouse
warehouse.exe

# Frontend build (rebuilt via `pnpm build`)
frontend/dist/
# node_modules (installed via pnpm install)
node_modules/

# Go test binaries
*.test

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swpbuild/
frontend/package.json.md5
